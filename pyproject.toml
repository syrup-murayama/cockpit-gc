[project]
name = "cockpit-gc"
version = "0.1.0"
description = "Read-only AGI Cockpit task cleanup reporter"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
cockpit-gc = "cockpit_gc.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
