[project]
name = "cockpit-gc"
version = "0.1.0"
description = "Conservative AGI Cockpit task cleanup reporter and review helper"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
cockpit-gc = "cockpit_gc.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
